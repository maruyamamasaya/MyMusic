---
status: completed
date: 2026-08-29
---

# ホームのチューニングタグ調整

## 変更

- ホームのチューニング表示をカード型タイルから、高さ44ptの1行タグへ変更した。
- プリセットごとに決定的に選ばれる7種類のカラフルなグラデーションを設定した。
- 横スクロールで全プリセットを直接選択できるよう、ホーム表示の10件制限と「続きを見る」タイルを廃止した。
- 適用中のタグはチェックマークと白い枠線で判別できるようにした。
- チューニング欄を「マイミュージック」の直前へ移動し、マテリアル背景と枠線を持つ独立したカードにした。
- カード右上に現在適用中のプリセット名を表示し、該当がない場合は「未選択」と表示するようにした。
- ホームのチューニングタグ先頭に「全曲表示」を常設し、設定中の表示にも反映した。
- 「ジャンルごとの表示」でプリセットを編集モードからドラッグして並び替えられるようにし、その順序を永続化した。
- 新規作成・名称変更時の自動並び替えを廃止し、ユーザーが決めた順序を維持するようにした。
- プリセット適用後に「設定しました」アラートと、適用したプリセット名を表示するようにした。

## 検証

- `git diff --check`: 成功。
- iPhone 17 / iOS 26.5 Simulatorで `GenreDisplayPresetOrderingTests` 1件が成功し、対象を含むDebug buildも成功した。
- 並び替え後のプリセット順がStoreの再生成後も保持されることをUnit Testで確認した。
- 初回検証で右上ラベルの計算プロパティにコンパイルエラーを検出し、明示的なreturnを追加して再検証した。

## 文書判断

- Store / Service、データフロー、永続化、製品の現在状態は変わらないため、`CURRENT.md` と `ARCHITECTURE.md` は更新していない。
- 長期的な設計判断は生じていないため、ADRは追加していない。
